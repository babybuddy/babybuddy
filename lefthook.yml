pre-commit:
  parallel: true
  commands:
    black:
      glob: "*.py"
      run: pipenv run black --check --diff --color {staged_files}
    ruff:
      glob: "*.py"
      run: pipenv run ruff check {staged_files}

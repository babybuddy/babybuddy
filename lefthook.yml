pre-commit:
  parallel: true
  commands:
    black:
      glob: "*.py"
      run: uv run black --check --diff --color {staged_files}
    ruff:
      glob: "*.py"
      run: uv run ruff check {staged_files}
